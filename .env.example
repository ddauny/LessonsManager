# Flask configuration
FLASK_ENV=production
FLASK_SECRET=your-secret-key-here-change-this-in-production

# Google Calendar OAuth (required for calendar integration)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Calendar API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URIs: https://your-domain.com/oauth2callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# FinTrack Integration (optional)
# If these are not set, FinTrack sync will be skipped
FINTRACK_URL=http://localhost:3000
FINTRACK_TOKEN=your-fintrack-bearer-token
FINTRACK_ACCOUNT_ID=your-fintrack-account-id

# Production settings
# When deploying to production:
# 1. Set FLASK_ENV=production
# 2. Generate a strong FLASK_SECRET (e.g., using: python -c "import secrets; print(secrets.token_hex(32))")
# 3. Use HTTPS for your domain (required for Google OAuth)
# 4. Update Google Cloud Console redirect URIs to use your production domain
